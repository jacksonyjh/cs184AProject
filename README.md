# cs184AProject

Dataset used:
https://www.kaggle.com/datasets/uciml/breast-cancer-wisconsin-data?select=data.csv

download this dataset, save the path 
download the notebook

In the ipynb, replace "PATH" with the path of the downloaded datset

After clicking "Run all Cells", model should run.
